/*
 * Archivo principal de JS
 */

// SCROLL
var lastScrollTop = 0;
	window.addEventListener("scroll",function(){
		var currentScroll = window.pageYOffset|| window.scrollY ||window.body.scrollTop;
		if(currentScroll>lastScrollTop){
			classList.add
			
		}
		else{
			console.log("up");
		}
		lastScrollTop = currentScroll;
	});
